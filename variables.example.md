# Variables de entorno (ejemplo)
#
# Copiá esto a un archivo local NO versionado (por ejemplo `variables.md` o `.env`)
# y completá los valores.
#
# IMPORTANTE: nunca commitear secretos (client_secret, API keys, passwords).

# Backend (Rails)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# OAuth / CORS
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
CORS_ORIGINS=http://localhost:5173
FRONTEND_URL=http://localhost:5173

# Opcional (si en algún momento se integra)
RESEND_API_KEY=
RESEND_FROM_EMAIL=
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

