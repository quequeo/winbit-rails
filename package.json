{
  "name": "winbit-rails",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix ui ci --include=dev && npm --prefix ui run build && node scripts/copy-ui-dist.mjs",
    "heroku-postbuild": "npm run build"
  }
}

