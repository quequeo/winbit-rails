{
  "name": "winbit-rails",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix ui ci --include=dev && npm --prefix ui run build && rsync -av --delete --exclude 'robots.txt' --exclude 'cache-bust.txt' ui/dist/ public/",
    "heroku-postbuild": "npm run build"
  }
}

